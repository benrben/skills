{
  "mcpServers": {
    "arch-map": {
      "command": "uv",
      "args": ["run", "--directory", "${CLAUDE_PLUGIN_ROOT}/fathom/arch-map", "arch-map"],
      "env": {
        "ARCH_MAP_DATA_DIR": "${CLAUDE_PLUGIN_DATA}"
      }
    }
  }
}
